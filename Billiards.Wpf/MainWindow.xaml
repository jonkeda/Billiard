<Window x:Class="Billiards.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:Billiard.viewModels" 
        xmlns:views1="clr-namespace:Billiard.views"
        d:DataContext="{d:DesignInstance IsDesignTimeCreatable=false, Type={x:Type viewModels:MainViewModel}}"
        mc:Ignorable="d"
        WindowState="Maximized"
        WindowStyle="SingleBorderWindow"
        Title="Billiards"
        >

    <TabControl>
        <TabItem Header="Table">
            
        </TabItem>

        <TabItem Header="Filter view">
            <views1:FilterView DataContext="{Binding FilterViewModel}" />
        </TabItem>

        <TabItem Header="Video">
            <views1:CaptureView DataContext="{Binding CaptureViewModel}" />
        </TabItem>

    </TabControl>
</Window>
